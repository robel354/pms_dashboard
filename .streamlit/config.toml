[server]
headless = true
enableCORS = false
enableXsrfProtection = false
# Do not hardcode `port` for Streamlit Community Cloud.
# The platform expects the app to bind to its provided port (defaults to 8501).

[browser]
gatherUsageStats = false
